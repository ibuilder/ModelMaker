# Mobile build (`npm run build:mobile`, vite --mode mobile). A phone has no local backend, so set
# this to your hosted API origin (the Pro/cloud tier). Leave blank for a viewer-only offline build
# (open .frag/.ifc locally; portal/proforma need the API).
VITE_API_URL=
